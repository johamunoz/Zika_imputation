---
title: "Untitled"
output:
  word_document: default
  html_document: default
  pdf_document: default
---


```{r echo=FALSE, results='hide',message=FALSE}
rm(list=ls()) # clean environment

# Data manipulation packages
library(here) 
library(data.table)
library(dplyr)
require(knitr)
library(summarytools)
library(magrittr)

load(here('3_Output_data','pldata.RData'))
infoexp <- as.data.table(readxl::read_xlsx(here('1_Input_data','Infoexp.xlsx'),sheet="Table")) #CSV file 
var_inc<-infoexp[Inclusion==1,Variable] #Variables to work with
data<-as.data.table(pldata)[,colnames(pldata)%in%var_inc,with = FALSE]
data<-data[,-c("studyname","mid_original","childid_original")]
```

```{r setup, results='asis'}
#data %>% select(studycode) %>% dfSummary(, plain.ascii = FALSE, style = "grid", tmp.img.dir = "/tmp")
data %>% group_by(studycode) %>% dfSummary(na.col = FALSE)
```



